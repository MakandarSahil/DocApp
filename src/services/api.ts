// import {API_URL} from '../utils/config';

// export const getDocuments = async (status: string, role: string, query: string) => {
//   const url = `${API_URL}/documents?status=${status}&role=${role}&query=${query}`;
//   const response = await fetch(url);
//   return response.json();
// }
